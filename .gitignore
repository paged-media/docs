# Node / pnpm
node_modules/
*.tsbuildinfo

# Next.js
.next/
out/
next-env.d.ts

# Fumadocs generated source index
.source/

# Cross-repo data pulled + projected by `pnpm generate:docs` (scripts/generate/).
# Regenerated on every build from the pinned sources (sources.pin) — never committed.
.generated/

# generated example barrel + assembled artifacts
examples/.generated/
dist/

# OS / editor / runtime
.DS_Store
.vscode/
.idea/
.claude/

# DEFENSIVE — the IDML spec is copyrighted vendor material and must NEVER land
# in this PUBLIC repo (clean-room §6.1). Never reference it by path either.
*.pdf
idml-specification*

# clean-room baseline is fetched privately into CI, never committed
cleanroom/.baseline/

# Built WebGPU preview wasm (built by scripts/build-preview-wasm.sh from a core checkout)
public/preview/

# Live-app demo sessions pulled from editor release assets (scripts/generate/pull-sources.mjs)
public/demos/
