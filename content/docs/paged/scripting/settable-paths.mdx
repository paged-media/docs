---
title: Settable paths & id grammar
description: The property paths accepted by paged.set, and the id grammar for addressing elements — generated from the engine's scripting catalog.
tier: pro
diataxis: reference
status: published
owner: paged-docs
---

**In short:** how you address an element (the **id grammar**) and what you can
change on it (the **settable paths**, the second argument to `paged.set`). Both
are generated from the engine catalog and stay in lockstep with the mutation
layer.

## Addressing elements

<ScriptingCatalog section="grammar" />

## Settable property paths

Grouped by theme, with the IDML type hint and description where a path maps to a
known attribute. Each group links to its runnable [showcase](/docs/paged/scripting/showcases).

<PathReference />
