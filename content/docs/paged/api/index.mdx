---
title: API reference
description: Every programmable surface of paged in one place — the paged.* scripting API, the paged command line, the Viewer SDK, the editor-server REST API, and the plugin manifest — each generated from its own source of truth.
tier: intermediate
diataxis: reference
status: published
owner: paged-docs
reviewed: "2026-06-27"
---

**In short:** paged exposes five programmable surfaces. Each reference below is
**generated from its own source of truth** — the engine's scripting catalog, the
CLI's own argument parser, the SDK package surface, the server's OpenAPI spec,
and the plugin manifest schema — so none of them drift from the running code.

<ApiReferenceIndex />

## Which one do I want?

- **[Scripting (`paged.*`)](/docs/paged/scripting)** — read and author the open
  document from inside the editor's script panel, the REPL, or headless via
  `paged-run`. Start with the [examples](/docs/paged/scripting/examples).
- **[Command line (`paged`)](/docs/paged/cli)** — the same engine from a shell:
  author with a script, render, export, read back what a document holds, and
  verify it deterministically. The lane a build system wants.
- **[Viewer SDK](/docs/paged/sdk)** — embed a WebGPU IDML viewer on a canvas:
  load a document, drive the camera, render to bytes, listen for events.
- **[REST API](/docs/paged/api/rest)** — the editor-server HTTP surface for
  accounts, document storage, billing, and assets.
- **[Plugin manifest](/docs/paged/plugin-sdk)** — the declarative contract a
  plugin bundle ships to contribute tools, panels, commands, and document
  behaviour.

## What each one reaches

They all sound complete, and they are not equal. This table is generated from
the capability × surface matrix, whose every cell comes from the gate that owns
that surface — the engine's own CLI and scripting surface tests, the editor's
probed capability table, each SDK's generated catalog. Nothing in it is written
by hand, including the reasons a cell is not reachable.

<SurfaceReach />

The two populations are the engine's own: an **authoring operation** is one
`Mutation` — insert text, resize a frame, create a swatch — and a **message
kind** is one request on the worker channel, which covers the reads, the
exports, the gestures and the container parts as well as the writes. A surface
that reaches every operation and half the message kinds can author anything and
ask fewer questions about it.

The **editor itself** is not a sixth entry here, and the reason is worth saying
rather than leaving as an omission: it has no API of its own. Everything it can
be driven to do it is driven to do through two of the surfaces above —
`paged.*` scripting for the document, and the plugin manifest for the interface
around it. A reference for "the editor" would be a third statement of those
two.
