---
title: Extract all text from a document
description: Walk every story and concatenate its content.
tier: intermediate
diataxis: how-to
status: stub
owner: paged-docs
---

To pull the plain text out of a document, read the design map's story list, open
each `Stories/Story_*.xml`, and concatenate the `Content` of every range in order,
treating `Br` as a line break.

> 🚧 Being written — a worked recipe over a multi-story example.
