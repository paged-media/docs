---
title: Sandbox & budgets
description: The execution model for paged.* scripts — a pure ECMAScript sandbox with no I/O, plus the runtime budgets that keep scripts safe — generated from the engine catalog.
tier: pro
diataxis: reference
status: published
owner: paged-docs
---

**In short:** scripts run in a **pure ECMAScript sandbox** — no network, no
filesystem, no timers, only `paged.*` and `console.*`. The rules below come
straight from the engine catalog.

<ScriptingCatalog section="constraints" />
